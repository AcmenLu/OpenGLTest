#version 330 core
in vec3 mColor;
in vec2 mTexCoord;
out vec4 FragColor;

uniform sampler2D texture1;
uniform sampler2D texture2;
void main()
{
	// FragColor = mix(texture(texture1, mTexCoord), texture(texture2, mTexCoord), 0.2);
	FragColor = texture(texture1, mTexCoord);
}